# 지역 도메인 개선 — Phase 2 정리 (/region 어드민·유저페이지·테마·SEO)

작성일: 2026-08-31
상태: **초안** — Phase 1(에픽 COM-2574 T1~T8)이 데이터·필터·표기 토대를 까는 동안, 그 위에 올릴 **유저 표면 + SEO** 트랙을 4축으로 정리. Jira 티켓화 전 스코프 합의용.

> 🧭 마스터 [260701] §5·§6의 실행 스포크. Phase 1 실행 구조 = `[260806] region-epic-task-breakdown.md`. 본 문서는 그 에픽분해가 "Phase 2 (별도, TBD)"로 미뤄둔 **/region 어드민·유저페이지·테마·SEO**를 정본으로 소유.
> 상세 설계 정본은 여전히 각 스포크가 소유(한 사실 한 곳): IA·노출방식 = [260622] / 서울 테마 매핑 = [260623] / 지리모델 = [260701 geo-model]. 본 문서는 **Phase 2 범위·구조·순서·열린 결정**을 소유.

---

## 0. Phase 1 vs Phase 2 경계

| | 소유 | 상태 |
|---|---|---|
| **Phase 1** (에픽 COM-2574 T1~T8) | CITY·DL·REGION 재정비 + 스팟 리스트 필터·표기 전환 | T1 완료 · T2·T3·T4 진행 중 |
| **Phase 2** (본 문서) | `/region` 어드민 CMS + 유저페이지 UI + 테마 축 + SEO | 스코프 정리 중 |

- Phase 2는 **Phase 1 데이터 생성 완료를 전제**로 배포한다(마스터 원칙: "깨진 페이지에 트래픽 먼저 붓지 않음").
- **비치헤드 = 서울**. Q3 OKR KR1 = "SEO 친화 지역 페이지 라이브(서울)". ⚠️ 이건 **측정·집중 초점이지 배포 상한이 아님** — 배포 게이트는 도시 allowlist 없이 페이지 단위 조건(§5-1). Phase 1 완료 시 REGION ≈45개 전부 유효 대상.
- ⚠️ 넘버링 주의: restructure [260622]가 부르던 "Phase 2(REGION/CITY 생성)"는 이미 Phase 1의 **T2(picker)·T3(REGION 생성)**로 흡수됨. 본 문서의 Phase 2 = restructure의 **Phase 3(BE) + Phase 4(UI) + 어드민 횡단 + SEO**.

---

## 1. 축① — 어드민 (`/region` CMS)

콘텐츠·노출을 운영자가 채우는 도구. **유저페이지(축②)의 데이터 소스** → Phase 2 안에서 선행.

| ID | 작업 | 마스터/스포크 앵커 |
|---|---|---|
| AD-CITY | `/region` **도시(CITY) 탭** — 카드메타(대표이미지·태그·설명)·slug·**도시간 이동 편집**(수단별 time/cost per 상대도시)·자동번역·노출/숨김. **노출 대상 = 자식 REGION 보유 CITY만**, 카드메타 미입력 = 비활성 | [260701] §3-3① · [260622] AD-1·AD-2 · 어드민 WF |
| AD-REGION-META | `/region` **지역(REGION) 탭 — 지역 메타정보 입력**(신설, CITY 탭과 대칭). 히어로 이미지·태그·한 줄 소개·지역 설명·slug·자동번역 + 주요 역(역별 노트)·공항 접근·persona. 유저페이지 `/region/{city}/{zone}` 시안에서 역산 — 상세 = **§1-1** | 시안 `#region-domain-renewal` `/region/seoul/gangnam` · 미팅 2026-09-03 |
| AD-REORDER | **카테고리 순서 변경** — 도시 상세·지역 상세 각각에서 [대카 순서 + 각 대카 내 중카 순서] 드래그 재정렬(독립·D). 기본=home-nav/category priority. **묶기(grouping)는 제거·후속**(§3-2) | [260622] AD-4 · BE-5(경량) |
| AD-SLUG-DICT | **예약 카테고리 → 안정 slug 매핑 사전**(전역, 기본 leaf용) 관리 | §3-3 · BE-3 |
| AD-SLUG | 슬러그 레지스트리 유니크 가드 — zone↔theme slug 충돌 생성 차단 | [260622] AD-6 · BE-3 |

- **운영 불변식(Phase 1에서 이미 확정)**: REGION 노출 ⟹ 부모 CITY 활성화(카드메타). 미활성 CITY 아래 REGION 게시 차단.
- CITY **엔티티 생성·이름**은 분류 레이어(`/location`) 소관. `/region`은 **콘텐츠 레이어**(노출값)만 편집.

### 1-1. AD-REGION-META — 지역(REGION) 메타 필드 (확정 2026-09-03)

유저페이지 시안 `/region/seoul/gangnam`의 각 요소를 어드민 입력으로 역산한 결과. **CITY 탭과 대칭 구조**로 REGION 탭에 "지역 메타정보 입력" 섹션 신설.

**유저페이지 요소 → 어드민 필드 매핑**

| 유저페이지(지역 상세) | 어드민 | 비고 |
|---|---|---|
| 히어로 대표 이미지(풀폭) | ➕ **히어로 이미지** | 기존 `리스트 이미지 228×128` **규격 교체**(구역 진입=지도 폴리곤 라벨이라 카드 썸네일 미사용) |
| tagline(h1 밑 한 줄) | ➕ **한 줄 소개** | |
| 설명 문단 | ✅ **지역 설명**(현행 유지) | SEO 가드레일 "에디토리얼 인트로(thin 방지)"를 채우는 필드 |
| `#쇼핑 #K-뷰티 #코엑스` | ➕ **태그** | CITY와 대칭 |
| breadcrumb·URL | **slug — 영문 지역명에서 자동 생성**(편집 필드 없음) | 아래 ◆ |
| 이동 > 주요 역 | ✅ **주변 지하철역 — 현행 수동 연결 유지** | 아래 ★ |
| 이동 > 공항 접근 | ➕ **공항 접근 정보** | CITY 상속 + REGION override(선택) |
| "이런 여행자에게 추천" 카드 | ➕ **persona**(아이콘·라벨·설명·관련 스팟) | 미입력 시 섹션 미노출 |
| 상세 지도(동 경계) | ✅ **지도 뷰 공개 토글**(입력 없음) | 폴리곤 = 선택 법정동 union **자동 도출** |
| 블로그 섹션 | — | `blog.region` 참조 **자동 집계**(BE-8) |
| 테마 캐러셀 순서 | AD-REORDER | §3-2 |
| 교통패스·유심 칩 | — | 전역 여행필수 모듈 **자동**(`[260901]`) |

**◆ slug = 영문 지역명 자동 생성 (확정 2026-09-03)**
- **어드민에 slug 편집 필드를 두지 않는다.** 영문 지역명(`region_translation` en)에서 slugify(소문자화·공백→하이픈·영숫자와 하이픈 외 제거)해 자동 생성. 영문명은 어차피 필수 입력이라 **추가 입력 비용 0**이고, slug↔표시명 불일치가 구조적으로 불가능해진다.
- **REGION 이름은 생성 시 입력, 생성 후 수정 불가**(옵션 A 확정 — 리브랜딩은 고려하지 않음). 이름이 불변이므로 **slug도 자동으로 불변** → 별도 freeze 로직 불필요, URL·SEO 안정.
- **생성 시 이름 특수문자 차단**(입력 검증). `압구정/신사` 같은 슬래시가 URL 경로를 깨뜨리는 것을 입력 단계에서 막는다.
- **`(city, slug)` 유니크 가드는 그대로 필요**(AD-SLUG) — 3번째 칸이 zone과 카테고리 leaf의 **공유 네임스페이스**(`/region/seoul/gangnam` vs `/region/seoul/restaurants`)라 자동 생성이어도 충돌하면 저장 차단 + 안내.
- 부수 효과: **영문명 누락 = slug 부재 = 게시 불가**가 자연히 성립(별도 검증 불요).
- ⚠️ **Phase 1 정비 시점에 영문명을 확정·교정해야 한다** — 생성 후 못 고치므로. 실측 오타 존재: **압구정의 영문명이 `Yeouido`로 입력**, **여의도 영문명 빈칸**(`[260730]` 데이터 정합 메모). 방치하면 압구정이 `/region/seoul/yeouido` 로 열린다. 이름 변경 논의가 걸린 건(`한남 → 용산`, `[260806] jira-paste`)도 **생성 전에** 결론내야 한다.

**★ 주요 역 = 현행 수동 연결 유지 (확정 2026-09-03)**
- 구조: `MAIN_SUBWAY`(호선 11) → `MIDDLE_SUBWAY`(역 478, `parent_code` 연결). 역·호선 이름은 `category_translation`으로 **이미 다국어**. 저장 = 기존 `region_has_subway`. **신규 스키마 0.**
- **자동 도출 검토했으나 기각** — 스팟 역 태깅(4,103 스팟 / 21,756 링크)으로 랭킹하면 강남은 잘 나오지만(시안 수동 선정 5개 중 4개 일치), **커버리지가 사실상 서울 전용**이라 전국에서 안 됨:
  - 영업중·공개·예약가능 스팟 1,824개 중 역 태깅 보유 1,182개(64.8%). 근데 시도별로는 **서울 89.0%(1,151/1,293) vs 부산 0.6%(1/171) · 강원 0% · 제주 3.1% · 경기 10.9% · 인천 32.2%**, 대구·대전·광주·충청·전라·경남 전부 0.
  - 원인 = **호선 카탈로그가 수도권 전용**(1~9호선·수인분당·공항철도). **부산·대구·광주·대전 지하철이 카탈로그에 아예 없음** → 자동이든 수동이든 그 지역은 역을 붙일 수 없음.
- ⇒ 운영자 수동 연결 유지. **연결 역 0개면 유저페이지 '주요 역' 섹션 미노출**(부산·제주·강원 REGION은 역 섹션 없이 성립 — 스팟·블로그·지도로 페이지는 온전함).
- **역별 한 줄 노트는 v1 제외** — 15개 언어 번역 테이블이 새로 필요해 비용이 큼(역명·호선은 기존 다국어라 공짜). 후속.
- ⚠️ 표시 처리: ① 같은 역이 **호선별 별도 row**라 역명 기준 머지 필요(`강남구청 · 7호선·수인분당선`) ② 호선 이름이 ko 로케일에도 영어(`Line 2`·`Suin·Bundang Line`) → 번역 보정 대상.
- **후속 별 트랙**: 비수도권 호선·역 카탈로그 확충(부산 1~4호선 등) + 스팟 재태깅. Phase 2 블로커 아님.

- **활성화 게이트 = 히어로 이미지·태그·설명**(CITY의 image·tags·desc와 대칭). 부모 CITY 가드는 그대로 별도 유지.
- **자동번역 버튼을 REGION 탭에도** — 한 줄 소개·설명·태그·persona·역 노트 전부 언어별.
- **초기 백필은 AI** — 텍스트 계열은 AI 초안으로 채움. 유일한 병목 = **히어로 이미지**(실사진 필요, 소싱 방식 별도 결정).

**삭제 확정**

| 필드 | 근거 |
|---|---|
| 리스트 이미지 228×128 | 쓰는 화면 없음(구역 진입=지도 라벨 / `/region` 인덱스 대표이미지=CITY껏). 히어로로 승격 |
| **지역별 리뷰 공개 여부** | 아래 참조 |
| 지역 주소 | 유저페이지 노출 없음. 범위=법정동, 지도=폴리곤 자동 |
| (기확정) SEO 3종·도메인 연결·스팟 직접연결·플레이스 연결 | RG-3 정리 대상 · 스팟은 법정동 자동 소속 |

**⚠️ 지역 리뷰 — 어드민 토글만 제거, 테이블·적재는 유지 (실측 2026-09-03)**
- `region_review`(7,412행) = 유저가 지역에 직접 쓰는 리뷰가 **아님**. 스키마에 본문 없음(`is_recommend` 1/0), 본문은 `region_review_translation.comment`(111,180행, 15개 언어 자동번역). 실제 내용은 **스팟 후기**("미용실 분위기 좋았다"·"기사 서비스 친절"·"아트메이크 최고") — 스팟 리뷰가 그 스팟 위치의 법정동으로 복제 적재되는 구조.
- **2025-03에 소스 전환** — 그전엔 `region_id` 채움, 이후 **전량 null·`legal_location_legal_code`만** 채움. 즉 region_id 기반 연결(어드민 토글이 제어하던 것)은 **1년 반째 신규 0**. 살아있는 5,848건(오늘도 적재)은 REGION 엔티티와 무관.
- ⇒ **어드민 토글 제거 안전.** 단 **테이블·적재 파이프라인은 건드리지 말 것**(스팟 리뷰 쪽이 깨짐). legal_code 축은 REGION 스팟 노출 축과 동일하므로, 후속에 "이 지역 여행자 후기"를 붙이려면 region_id 없이 법정동 매칭만으로 복원 가능.

## 2. 축② — 유저페이지 (FE)

| ID | 작업 | 앵커 |
|---|---|---|
| FE-ROUTE | 4단 라우팅(`/region` → `/{city}` → `/{city}/{zone}` → theme leaf) + 3번째 칸 zone\|theme 분기 | [260701] §5-1 · [260622] FE-1 |
| FE-301 | 기존 `/spot/region/{숫자 id}` → 새 slug **301** (498 URL SEO 승계) | FE-2 |
| FE-INDEX | `/region` 도시 인덱스 — 한국 지도 그리드 + **선택 가능 = 활성화 CITY만**(§2-1) + 도시 선택 시 우측 패널 REGION 진입점 | [260701] §5-1 |
| FE-CITY | `/region/{city}` — 구역(zone) 그리드 + 도시간 이동 + **콘텐츠 영역(스팟·블로그)** + 테마 캐러셀. **콘텐츠 = CITY 스코프, REGION 무관**: 스팟=CITY 스팟 ∩ 테마(**B-2** 완전집합), 블로그=구→CITY 롤업 집계(**BE-8**, theme 독립) | FE-3 · [260701] §3-3① |
| FE-ZONE | `/region/{city}/{zone}` — 섹션·subway·persona·구역 지도 | FE-3 |
| FE-LEAF | 테마 leaf(city×theme·zone×theme) — 풀 그리드 + 에디토리얼 인트로 + 폴리곤 렌더 | FE-4 |
| FE-MOVE | 다도시 배분 + **도시간 이동 카드** 모듈(다도시 27.4%, `seoul busan`) — 서울 페이지 최상위 가치, 교통 공급부족 보완 | [260623] §5 |
| FE-NEW | **신규 스팟 노출 영역** — 도시·지역 페이지 내 별도 영역. **기준 = `spot.created_at` 최근 3개월 · 범위 = 그 페이지 스코프 그대로 · 개수 = 카테고리 캐러셀과 동일 9개 · 0건이면 섹션 미노출**(§2-2) | 미팅 2026-09-02 · 확정 2026-09-03 |

### 2-1. CITY ↔ REGION 포함관계 · 노출 규칙 (확정)

REGION이 상위 CITY를 부모 값으로 갖게 되면서(`region.city` FK) CITY⊃REGION 포함관계가 생긴다. 이 위에서 CITY 노출을 **2단 게이트**로 건다.

| 게이트 | 조건 | 결과 |
|---|---|---|
| ① 어드민 노출 | CITY가 **자식 REGION ≥1** 보유 | 어드민 `/region` **도시 탭에 등장**(초기 비활성) |
| ② 유저 노출 | 위 + **카드메타(대표이미지·태그·설명) 입력·저장** | CITY **활성화** → 유저 `/region` 지도 그리드에서 **선택 가능** |

- **한 방향 불변식 유지**: REGION 게시 ⟹ 부모 CITY 활성화(어드민 WF의 "REGION만 켜고 도시 메타 누락 고아 차단"). 여기에 **역방향 그리드 게이트 추가**: 자식 REGION 없는 CITY는 도시 탭·유저 그리드에 아예 안 뜸.
- **부산 라이프사이클(예시)** — ⚠️ 마스터 §3-3①의 "부산처럼 REGION 없이 활성화되는 CITY" 서술은 이 규칙으로 **개정**된다(REGION 전무 CITY는 그리드 비노출).
  1. T3: 부산 **도시 전체 REGION은 해체**(시도 전체 REGION 금지)하되, **서면·광안리·감천동·청사포 등 구역(zone) REGION을 신규 생성**(`[260730]` 초안: 서면=부산진구 부전동·전포동 / 감천동=사하구 감천동 …).
  2. 부산 CITY가 자식 REGION을 갖게 됨 → 어드민 도시 탭에 **비활성 상태로 등장**.
  3. 운영자가 부산 카드메타 입력·저장 → **활성화** → 유저 `/region` 그리드에서 부산 **선택 가능**, `/region/busan`에서 zone 그리드(서면·감천…) 노출.
- **결정 = 옵션 (A)**: CITY 그리드 노출 = 활성화 AND 자식 REGION ≥1. (이전 열린질문 "부산 그리드 노출 여부" 해소 — 부산은 zone REGION 보유라 자연히 노출 경로 확보.)

### 2-2. FE-NEW — 신규 스팟 영역 (확정 2026-09-03)

| 항목 | 규칙 |
|---|---|
| 기준 | **`spot.created_at` 최근 3개월** |
| 범위 | **그 페이지 스코프 그대로** — 도시 = CITY 스팟(B-2) / 지역 = 선택 법정동 스팟. 기존 노출 규칙 재사용, 신규 조회 로직 없음 |
| 개수 | **9개** — 카테고리 캐러셀(monthly best 9)과 동일 |
| 빈 경우 | **0건이면 섹션 미노출**(신규 등록 없는 지역 대비) |

- ⚠️ **기존 'new 태그' 로직은 재사용 불가 — 코드베이스에 없음**(2026-09-03 확인). 유사한 것들은 전부 다른 물건: `CategoryFlag{BEST,NEW,HOT}` = **카테고리** 배지(Redis 캐시 `main:category:flag:{lang}`, 계산 배치는 product 레포 밖) · `HomeNavigationBadgeType.NEW` = 홈 네비 **수동** 배지 · `spot.repo.ts` 90일 = 쿠폰 인기 스팟의 **예약** 기준 · `spot_tag` = `tag_code` 참조 태그(생성일 무관). ⇒ **신규 정의**로 간다.
- 3개월은 노출량 보고 조정할 값 — 상수 하나로 빼둔다.

## 3. 축③ — 테마 · 스팟 그룹핑

### 3-1. 페이지별 그룹핑 구조 (확정 2026-08-31)

5개 페이지 타입이 **예약 카테고리 택소노미**로 한 단계씩 내려가며 그룹핑(기본). REGION 레벨은 CITY 규칙의 region-필터 미러:

| 페이지 | 스팟 범위 | 기본 그룹핑 축 |
|---|---|---|
| (a) `/region` | 없음(스팟 비노출) | — |
| (b) `/region/{city}` | 도시 스팟 | 예약 **대카테고리**(= home-nav 기본, 순서 변경 가능) |
| (c) `/region/{city}/{category}` | 도시 ∩ 대카 | 예약 **중카테고리** |
| (d) `/region/{city}/{region}` | 도시 ∩ region(선택 법정동) | 예약 **대카테고리** |
| (e) `/region/{city}/{region}/{category}` | 도시 ∩ 대카 ∩ region | 예약 **중카테고리** |

- **CITY 레벨(b·c)** = 예약 대카/중카 자동. 순서 = home-nav/category priority **기본 + 페이지별 순서 변경 가능**(§3-2).
- **REGION 레벨(d·e)** = 예약 택소노미 자동 그룹핑(스팟만 region 필터). 순서 변경만, **묶기 없음** → 기본 CMS 최소.

### 3-2. 큐레이션 = 페이지별 카테고리 순서 변경 (확정 2026-09-02, 미팅 반영)

큐레이션 장치 = **각 페이지에서 카테고리 섹션 순서를 운영자가 재정렬**. 묶기(grouping) 없이 **순서(order)만** override.

- **어드민 설정 = 도시 상세 / 지역 상세 페이지 각각에서 독립** — ① 대카 순서(그 페이지의 대카 섹션) + ② 각 대카 내 중카 순서(그 대카 leaf의 중카).
  - 예) **서울 상세**: `미식-체험-K뷰티…` + (미식 내)`한식-카페-오마카세…` / **강남 상세**: `체험-미식-K뷰티…` + (체험 내)`의상대여-한복스냅-운세…` → 서울·강남 **완전 독립**(D).
- **데이터 모델** `region_category_order(scope_type[city|zone], scope_id, parent_category, category_code, order)`
  - `parent_category` **NULL** → 그 페이지 **대카 순서** / **대카값** → 그 대카 leaf의 **중카 순서**.
  - 행 없음 = **기본값 폴백**(대카=home-nav priority, 중카=category.priority). **상속 없음(flat)** — 강남이 서울 순서 안 물려받음.
- **유저 렌더 = 캐러셀 + 더보기 → leaf 페이지**(§3-3, 인페이지 확장 아님). 어드민 설정(중첩 리스트)과 유저 렌더(페이지 이동)는 별개.
- CMS 부담 최소 — **드래그 재정렬만**, 새 엔티티·묶음·커스텀 slug 없음.

⚠️ **묶기(grouping) 기능 = 제거 (미팅 2026-09-02)** — 대카 여러 개를 하나로 묶는 "강남의 의료 = 메디컬+한의원" 식 큐레이션은 **도시·지역별로 일일이 관리하기 어렵다는 영업/제휴팀 의견**으로 v1에서 뺌. **추가 여지는 있음**(요청 시 재도입).
- 파급: `region_section`·대-레벨 묶음·중카 묶기·묶음 slug·nesting **전부 v1 제외.** leaf slug = 예약 카테고리 slug **고정**(커스텀 묶음 slug 없음) → SEO·라우팅 더 단순.
- ~~이전(하이브리드 C): zone별 대카 묶음 override(essential 예시)·중카 묶기 후속·스키마 nesting~~ → 미팅으로 폐기, 순서 변경으로 대체.

### 3-3. 노출·파생

- **노출 = 캐러셀 + leaf**: monthly best 상위 9 캐러셀 + 스팟 ≥10이면 "전체 보기" → leaf. ("캐러셀에 다 못 담으면 leaf" 단순 규칙, 비노출 구간 0)
- **`master_theme`·`region_section` 전부 불필요화** — 전역 테마 사전·묶음 CMS 없이, **"예약 카테고리 → 안정 slug 매핑 사전"** + **"(페이지, 카테고리) → 순서" 저장**만 필요. **BE-5 = 페이지별 카테고리 순서 저장**(묶기 제거로 최경량).
- **서울 CITY 테마**([260623]): home-nav 대카테고리 **개별 캐러셀**(체험·미식·명소&입장권…). 묶음(K-뷰티·웰니스 5코드)은 와이어프레임 컨셉일 뿐 — CITY는 단일 카테고리(§6 #3).
- **비-테마 모듈**(카테고리 섹션 아님): 도시간 이동·추천 렌즈·일정 동선(itinerary)·시즌 배지·거점 동네 칩([260623] §5).
  - **전역 「한국 여행 준비」 모듈**(교통·유심·여행서비스) — 지역무관 고정 노출. 소스 = **744 대카째 + (743·745 ∖ 지역성 중카)**. 대카 통짜 금지(743·745는 근교셔틀·안경·식당 등 지역 스팟 혼재), **중카 블랙리스트 규칙**으로 순수 전역 ≈78개 자동 추출. **위치 = 카테고리 캐러셀 하단 · 노출 = 9개+더보기(monthly best 9 재사용) · 자체 URL 없음(더보기는 `/spot/list` noindex)**. 영역명은 기존 중카 `여행 필수`와 겹치지 않게 「한국 여행 준비」로 확정(2026-09-03). 데이터 조사·규칙 정본 = **`[260901] region-global-essentials-module.md`**.
- **BE**: leaf 쿼리(대카∩도시(∩region) → 중카 그룹) + 캐러셀 쿼리(monthly best 9) + 페이지별 순서 override 적용 = [260622] BE-4·BE-5(경량).

## 4. 축④ — SEO (가드레일 · 필수)

안 지키면 thin/중복으로 **역효과**. Phase 2의 모든 leaf는 아래를 만족해야 색인.

| 가드레일 | 규칙 | 앵커 |
|---|---|---|
| 콘텐츠 임계 | 스팟 **10개 미만** 테마 leaf는 생성·색인 안 함 | [260701] §5-2 · [260622] §3-5 |
| canonical | 테마 leaf가 정본. `/spot/list?category=` 필터뷰는 noindex/canonical 양보 | 〃 |
| 안정 slug | 예약 카테고리 → **안정 slug 매핑 사전**(leaf slug 고정). 묶기 제거로 **커스텀 묶음 slug 없음** — `(city[,zone],slug)` 레지스트리 유니크는 zone명↔카테고리 slug 충돌 방지용만 | 〃 · BE-3 |
| 301 승계 | 498 동네 URL → 새 slug 301(FE-301) | [260701] §5-1 |
| 에디토리얼 인트로 | leaf 상단 짧은 소개문(thin 방지) | [260622] FE-4 |
| 측정 | GSC 타깃 키워드셋(도시×테마) 노출·평균순위 — 권한 확보 후 | [260713] KR2 |

- **타깃 키워드**: '도시×테마'(seoul restaurants 월 1만~10만)·'도시×구역'(seoul gangnam), 합산 추정 월 ~50만([260701] §1).
- **KR1 산출물**: 서울 REGION 10개 × 테마 → **색인 테마 leaf 30개**([260713] KR1).

---

## 5. 순서·의존성

```
Phase 1 (T1~T8: 데이터·필터·표기)
   └─▶ 축① 어드민 CMS ─▶ 콘텐츠 입력(CITY 카드메타 · 페이지별 카테고리 순서는 선택)
          └─▶ BE(조회축·slug·leaf 쿼리·폴리곤) ─▶ 축② 유저페이지 + 축③ 테마 노출
                 └─▶ 축④ SEO 색인·측정(선행지표 → lagging, Q3말 물꼬·Q4 성숙)
```

- **어드민이 BE/FE보다 선행** — 데이터를 채워야 페이지가 성립(생성=어드민, 스크립트 아님).
- **유저 배포는 데이터 생성 완료 후** — Phase 1(T1~T8) 완료가 Phase 2 착수 전제. 어드민·유저페이지는 **동시 배포**(운영자가 어드민에서 메타를 채워야 유저페이지가 성립).
- **리스트→region 유입 위젯**([260713] KR3)은 지역 페이지 라이브 후 소프트오픈(내부 트래픽으로 전환 먼저 튜닝).

### 5-1. 배포 게이트 (확정 2026-09-03)

**전제**: Phase 1 정비 **완전 완료 후** Phase 2 착수. 따라서 "곧 삭제·변경될 REGION에 메타를 미리 붙이는" 위험은 없음. Phase 1 종료 시점 REGION ≈ **45개**, 전부 유효 대상.

**게이트 = 도시 allowlist 아님. 페이지 단위 조건 충족 시 자동 노출**:

| # | 조건 | 주체 |
|---|---|---|
| ① | 어드민(메타 입력) + 유저페이지 + **스팟 영역 자동 규칙** 구현 완료 | 개발 |
| ② | 해당 도시·지역 **카드메타 백필**(CITY=대표이미지·태그·설명 / REGION=히어로 이미지·태그·설명, §1-1) | 운영(초기 백필 AI) |
| ③ | **CITY 2단 게이트** 충족(자식 REGION ≥1 + 메타) — §2-1 | 자동 판정 |

- **①의 "스팟 영역 자동 규칙"** = B-2 쿼리 · 콘텐츠 임계 10 · monthly best 9 캐러셀 · 전역 여행필수 모듈 블랙리스트(`[260901]`). **운영자 입력은 0**이지만 개발은 필요 — 어드민과 함께 나가야 함.
- **순서 override(`region_category_order`)는 게이트 아님** — 미설정=폴백(대카 home-nav priority / 중카 category.priority)이라 안 채워도 페이지 성립. 배포 후 튜닝.
- **서울 한정 아님** — 45개 REGION 전부 유효(특히 부산: reddit 교통 수요 1위 도시, Phase 1에서 서면·감천 등 zone REGION 확보). 조건 맞는 도시·지역은 다 노출. 서울은 **OKR 측정 초점**(Q3 KR1 색인 leaf 30개)이지 배포 상한이 아님.
- **남은 건 결정이 아니라 처리 순서** — 45 REGION + 도시 메타 백필은 운영 작업량 이슈. 트래픽·수요 큰 순(서울→부산→…)으로 채우면 자연히 그 순서로 노출됨(제한이 아니라 진행 속도).
- **thin 리스크 없음** — 도시 페이지 스팟 = B-2 완전집합이라 어느 도시든 항상 충분. 임계 미달은 테마 leaf만이고 그건 **자동 비생성**(§4)이라 페이지가 깨지지 않음.

## 6. 열린 결정 (Phase 2 착수 전 정리)

**확정 (2026-09-02, 미팅 반영)**
- ✅ **큐레이션 = 페이지별 카테고리 순서 변경**(§3-2) — 도시·지역·leaf에서 카테고리 섹션 재정렬. **묶기(grouping)는 제거**(도시·지역별 관리 부담, 영업/제휴팀 의견). 추가 여지만 남김. ⇒ 하이브리드 C(대카 묶음)·중카 묶기 전부 폐기.
- ✅ **그룹핑 구조**(§3-1) = 도시/지역=대카 자동, leaf=중카 자동. 순서만 override.
- ✅ **CITY 순서 = home-nav 기본 + 페이지별 변경**(이전 "전역 1개·도시별 후속" → 순서 변경 v1 승격).
- ✅ **신규 스팟 노출 영역**(FE-NEW) 도시·지역 페이지에 추가. **노출 기준도 확정(2026-09-03)** — `created_at` 3개월 · 페이지 스코프 · 9개 · 0건 미노출(§2-2).

**남은 열린 결정**
1. ~~master_theme·묶기 관련~~ — **전부 해소/제거**: master_theme·region_section·묶기 없음(§3-2·3-3).
2. ~~카테고리 순서 override 스코프~~ — **확정(2026-09-02): D** = 도시·지역 상세 각각에서 대카 순서 + 대카별 중카 순서 독립 설정. flat 폴백(상속無). (§3-2)
3. **명소·교통 인벤토리 확충** — 최대 수요인데 공급 50·7, 임팩트 천장(별 트랙). [260623] §6-1
4. ~~**유저 페이지 배포 타이밍 게이트**~~ — **확정(2026-09-03)**: **도시 allowlist 없음. 페이지 단위 조건 충족 시 노출**(§5-1). Phase 1 완료가 전제라 45개 REGION 전부 유효 대상, 서울 한정 아님. 남은 건 결정이 아니라 **메타 백필 처리 순서**(운영 작업량 배분).
5. ~~**신규 스팟 영역 노출 기준**~~ — **확정(2026-09-03)**: `spot.created_at` 3개월 · 페이지 스코프 그대로 · 9개 · 0건 미노출. 기존 'new 태그' 로직은 코드에 없어 신규 정의(§2-2).
6. **(사실상 안 함) 카테고리 묶기 재도입** — Phase 2 범위 밖이고 **이후로도 도입 계획 없음**(2026-09-03). 가능성만 열어둠 — 요청이 오면 순서 저장 위에 묶음 레이어를 additive로 얹는 형태(별 엔티티 필요).

## 7. Jira 티켓화 (제안 · 미생성)

Phase 1 에픽(COM-2574)과 별개 에픽으로 분리 권장. 축별 태스크 초안:

| 태스크 | 축 | 선행 |
|---|---|---|
| P2-T1 `/region` 어드민 CMS(CITY 편집·**REGION 메타 §1-1**·페이지별 카테고리 순서 변경·카테고리→slug 매핑 사전·slug 유니크 가드) | ① | Phase 1 T2·T3 |
| P2-T2 BE — 조회축·폴리곤·slug 레지스트리·leaf/캐러셀 쿼리·CITY 집계(B-2) | ①③④ | P2-T1 |
| P2-T3 FE — 4단 라우팅·301·CITY/REGION 페이지·캐러셀+leaf·도시간이동 | ② | P2-T2 |
| P2-T4 SEO 가드레일·측정(canonical·임계10·GSC 셋업) | ④ | P2-T3 |

---

## 부록: 참조 스포크

**와이어프레임 (유저페이지 시각 소스, 2026-06-23)** — `https://wireframes-lkb.pages.dev/`
- 유저페이지 `#region-domain-renewal` — 5화면: `/region`(도시 인덱스·지도 그리드·도시간 이동표) · `/region/seoul`(구역 그리드+테마 캐러셀 5개+블로그) · `/region/seoul/restaurants`(테마 leaf, 하위 그룹 6개) · `/region/seoul/gangnam`(구역 상세: 섹션 5개+주요역+지도) · `/region/seoul/gangnam/restaurants`(zone×theme leaf)
- 어드민 `#region-domain-admin` — `/region` 도시(CITY)·지역(REGION) 2탭. CITY 탭=메타·도시간이동·자동번역·노출토글, REGION 탭=상세지역 선택→CITY 자동·지하철 검색·지도/리뷰 공개
- ℹ️ **와이어프레임은 컨셉 이미지** — 이대로 구현 확정 아님(대외 설명용). 실제 구성은 아래 문서 결정 우선.
- ⚠️ **WF↔문서 텐션(스펙 시 반영)**: ① CITY 테마 캐러셀이 "묶음"(볼거리·체험 = 명소3071+체험352)으로 그려졌으나 **CITY 테마축 = home-nav 단일 카테고리 전역 1개로 확정**(마스터 §5-2) → 실제는 체험·미식·명소&입장권이 **각각 별도 캐러셀**. 묶음은 컨셉 예시일 뿐 · ② leaf 하위 그룹핑 = **해소**(§3-1): leaf는 예약 중카테고리로 자동 그룹(한식/카페/시장…), zone은 대카 자동(+페이지별 순서 변경, 묶기는 제거·후속 §3-2). WF의 zone 큐레이션 섹션(쇼핑·나이트라이프)은 예약 카테고리 아닌 건 크로스셀로 빠짐 · ③ 교통·유심은 테마 아닌 링크 모듈(`/spot/list?category=743/744`, noindex 대상)

- 어드민 와이어프레임 수정 지시(§1-1 반영분): `[260903] region-admin-wireframe-instructions.md`
- 전역 "여행 필수" 모듈(교통·유심·여행서비스) 소싱 규칙·데이터 조사: `[260901] region-global-essentials-module.md`
- IA·노출방식·BE/FE/AD 작업분해 정본: `[260622] region-restructure-project.md`
- 서울 테마→카테고리 매핑: `[260623] region-seoul-theme-spec.md`
- 지리모델 결정: `[260701] region-geo-model-decision.md`
- Q3 OKR(측정·KR1 서울 라이브): `[260713] region-domain-q3-okr.md`
- Phase 1 에픽 구조: `[260806] region-epic-task-breakdown.md`
- 마스터 허브: `[260701] region-domain-improvement-master.md`
