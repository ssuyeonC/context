<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Free Itinerary Review - Creatrip</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    color: #24272B;
    background: #ffffff;
    line-height: 1.6;
  }

  /* ===== Header ===== */
  .header {
    position: sticky;
    top: 0;
    background: #ffffff;
    border-bottom: 1px solid #F2F3F5;
    padding: 0 1.25rem;
    height: 3.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 100;
  }
  .header-logo {
    font-size: 1.25rem;
    font-weight: 700;
    color: #00BDAD;
    text-decoration: none;
    letter-spacing: -0.02em;
  }

  /* ===== Hero ===== */
  .hero {
    background: linear-gradient(135deg, #edf6f5 0%, #ffffff 100%);
    padding: 3rem 1.25rem 2.5rem;
    text-align: center;
  }
  .hero-badge {
    display: inline-block;
    background: #00BDAD;
    color: #ffffff;
    font-size: 0.75rem;
    font-weight: 700;
    padding: 0.25rem 0.75rem;
    border-radius: 1.25rem;
    margin-bottom: 1rem;
    letter-spacing: 0.02em;
  }
  .hero h1 {
    font-size: 1.75rem;
    font-weight: 800;
    line-height: 1.3;
    margin-bottom: 0.75rem;
    color: #24272B;
  }
  .hero h1 em {
    color: #00BDAD;
    font-style: normal;
  }
  .hero p {
    font-size: 1rem;
    color: #9FA6AF;
    max-width: 28rem;
    margin: 0 auto;
  }

  /* ===== Counter ===== */
  .counter-section {
    padding: 1.5rem 1.25rem;
    text-align: center;
  }
  .counter-bar {
    max-width: 28rem;
    margin: 0 auto;
  }
  .counter-bar-track {
    background: #F2F3F5;
    border-radius: 0.5rem;
    height: 0.5rem;
    overflow: hidden;
    margin-bottom: 0.5rem;
  }
  .counter-bar-fill {
    background: linear-gradient(90deg, #00BDAD, #00D4C1);
    height: 100%;
    border-radius: 0.5rem;
    width: 37%;
    transition: width 0.5s ease;
  }
  .counter-text {
    font-size: 0.875rem;
    color: #9FA6AF;
  }
  .counter-text strong {
    color: #00BDAD;
    font-weight: 700;
  }

  /* ===== How it works ===== */
  .how-section {
    padding: 2rem 1.25rem;
    max-width: 32rem;
    margin: 0 auto;
  }
  .how-section h2 {
    font-size: 1.125rem;
    font-weight: 700;
    margin-bottom: 1.25rem;
    text-align: center;
  }
  .step-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .step {
    display: flex;
    align-items: flex-start;
    gap: 0.875rem;
    padding: 1rem;
    background: #F2F3F5;
    border-radius: 0.75rem;
  }
  .step-num {
    flex-shrink: 0;
    width: 2rem;
    height: 2rem;
    background: #00BDAD;
    color: #ffffff;
    font-size: 0.875rem;
    font-weight: 700;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .step-content h3 {
    font-size: 0.9375rem;
    font-weight: 700;
    margin-bottom: 0.125rem;
  }
  .step-content p {
    font-size: 0.8125rem;
    color: #9FA6AF;
  }

  /* ===== Form ===== */
  .form-section {
    padding: 2rem 1.25rem 3rem;
    max-width: 32rem;
    margin: 0 auto;
  }
  .form-section h2 {
    font-size: 1.125rem;
    font-weight: 700;
    margin-bottom: 1.25rem;
    text-align: center;
  }
  .form-group {
    margin-bottom: 1rem;
  }
  .form-group label {
    display: block;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 0.375rem;
    color: #24272B;
  }
  .form-group label .required {
    color: #00BDAD;
    margin-left: 0.125rem;
  }
  .form-group input[type="email"] {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 1px solid #E5E7EB;
    border-radius: 0.5rem;
    font-size: 0.9375rem;
    outline: none;
    transition: border-color 0.2s;
    color: #24272B;
    background: #ffffff;
  }
  .form-group input[type="email"]:focus {
    border-color: #00BDAD;
  }
  .form-group input::placeholder {
    color: #C5CAD0;
  }
  .form-group textarea {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 1px solid #E5E7EB;
    border-radius: 0.5rem;
    font-size: 0.9375rem;
    outline: none;
    transition: border-color 0.2s;
    resize: vertical;
    min-height: 10rem;
    font-family: inherit;
    line-height: 1.6;
    color: #24272B;
    background: #ffffff;
  }
  .form-group textarea:focus {
    border-color: #00BDAD;
  }
  .form-group textarea::placeholder {
    color: #C5CAD0;
  }
  .char-count {
    text-align: right;
    font-size: 0.75rem;
    color: #C5CAD0;
    margin-top: 0.25rem;
  }
  .char-count.valid {
    color: #00BDAD;
  }
  .form-hint {
    font-size: 0.75rem;
    color: #9FA6AF;
    margin-top: 0.375rem;
  }

  .submit-btn {
    width: 100%;
    padding: 0.875rem;
    background: #00BDAD;
    color: #ffffff;
    font-size: 1rem;
    font-weight: 700;
    border: none;
    border-radius: 1.25rem;
    cursor: pointer;
    transition: background 0.2s, transform 0.1s;
    margin-top: 0.5rem;
  }
  .submit-btn:hover {
    background: #00A89A;
  }
  .submit-btn:active {
    transform: scale(0.98);
  }
  .submit-btn:disabled {
    background: #C5CAD0;
    cursor: not-allowed;
    transform: none;
  }

  /* ===== Success State ===== */
  .success-message {
    display: none;
    text-align: center;
    padding: 3rem 1.25rem 4rem;
    max-width: 28rem;
    margin: 0 auto;
  }
  .success-message.show {
    display: block;
  }
  .success-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  .success-message h2 {
    font-size: 1.375rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
  }
  .success-message p {
    font-size: 0.9375rem;
    color: #9FA6AF;
    line-height: 1.6;
  }

  /* ===== Footer ===== */
  .footer {
    padding: 1.5rem 1.25rem;
    text-align: center;
    border-top: 1px solid #F2F3F5;
    font-size: 0.75rem;
    color: #C5CAD0;
  }

  /* ===== Responsive ===== */
  @media (min-width: 42.4375rem) {
    .header { height: 4.25rem; }
    .hero { padding: 4rem 2rem 3rem; }
    .hero h1 { font-size: 2.25rem; }
    .hero p { font-size: 1.125rem; }
    .form-section, .how-section { max-width: 36rem; padding: 2.5rem 2rem; }
  }
</style>
</head>
<body>

<!-- Header -->
<header class="header">
  <a href="https://creatrip.com" class="header-logo">Creatrip</a>
</header>

<!-- Hero -->
<section class="hero">
  <span class="hero-badge">LIMITED - FIRST 100 ONLY</span>
  <h1>Is your Korea itinerary <em>actually doable?</em></h1>
  <p>Our Korea travel experts will review your plan and give you honest, practical feedback — for free.</p>
</section>

<!-- Counter -->
<section class="counter-section">
  <div class="counter-bar">
    <div class="counter-bar-track">
      <div class="counter-bar-fill" id="counterFill"></div>
    </div>
    <p class="counter-text"><strong id="spotsLeft">63</strong> of 100 remaining</p>
  </div>
</section>

<!-- How it works -->
<section class="how-section">
  <h2>How it works</h2>
  <div class="step-list">
    <div class="step">
      <div class="step-num">1</div>
      <div class="step-content">
        <h3>Share your plan</h3>
        <p>Paste your itinerary in any format — text, notes, anything works.</p>
      </div>
    </div>
    <div class="step">
      <div class="step-num">2</div>
      <div class="step-content">
        <h3>Expert review</h3>
        <p>A Korea travel expert personally reviews your schedule within 24 hours.</p>
      </div>
    </div>
    <div class="step">
      <div class="step-num">3</div>
      <div class="step-content">
        <h3>Get feedback via email</h3>
        <p>Receive practical tips — timing, routes, must-book spots, and hidden gems.</p>
      </div>
    </div>
  </div>
</section>

<!-- Form -->
<section class="form-section" id="formSection">
  <h2>Submit your itinerary</h2>
  <form id="reviewForm">
    <div class="form-group">
      <label>Email <span class="required">*</span></label>
      <input type="email" id="emailInput" placeholder="your@email.com" required>
    </div>
    <div class="form-group">
      <label>Your itinerary <span class="required">*</span></label>
      <textarea id="itineraryInput" placeholder="Day 1: Arrive at Incheon, check in at Myeongdong hotel, visit Gyeongbokgung Palace...

Day 2: Bukchon Hanok Village in the morning, Hongdae area for shopping and lunch...

Paste your plan in any format — bullet points, paragraphs, whatever works for you!" required></textarea>
      <div class="char-count" id="charCount">0 / 50 characters minimum</div>
      <p class="form-hint">Include dates, locations, and activities for the best review.</p>
    </div>
    <button type="submit" class="submit-btn" id="submitBtn" disabled>Submit for review</button>
  </form>
</section>

<!-- Success -->
<section class="success-message" id="successMessage">
  <div class="success-icon">&#10003;</div>
  <h2>You're in!</h2>
  <p>We've received your itinerary. Our Korea travel expert will review it and send you detailed feedback within <strong>24 hours</strong>.</p>
  <p style="margin-top: 1rem;">Check your email — and don't forget to check spam, just in case.</p>
</section>

<!-- Footer -->
<footer class="footer">
  <p>&copy; 2026 Creatrip. No.1 K-Trend Platform for Global Travelers.</p>
</footer>

<script>
  const emailInput = document.getElementById('emailInput');
  const itineraryInput = document.getElementById('itineraryInput');
  const charCount = document.getElementById('charCount');
  const submitBtn = document.getElementById('submitBtn');
  const form = document.getElementById('reviewForm');
  const formSection = document.getElementById('formSection');
  const successMessage = document.getElementById('successMessage');
  const howSection = document.querySelector('.how-section');

  const MIN_CHARS = 50;

  function validateForm() {
    const emailValid = emailInput.value.includes('@') && emailInput.value.includes('.');
    const textValid = itineraryInput.value.length >= MIN_CHARS;
    submitBtn.disabled = !(emailValid && textValid);
  }

  itineraryInput.addEventListener('input', () => {
    const len = itineraryInput.value.length;
    charCount.textContent = `${len} / ${MIN_CHARS} characters minimum`;
    charCount.classList.toggle('valid', len >= MIN_CHARS);
    validateForm();
  });

  emailInput.addEventListener('input', validateForm);

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    submitBtn.disabled = true;
    submitBtn.textContent = 'Submitting...';

    // === Google Sheets 연동 포인트 ===
    // 아래 URL을 Google Apps Script Web App URL로 교체
    // const SHEETS_URL = 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec';
    // try {
    //   await fetch(SHEETS_URL, {
    //     method: 'POST',
    //     mode: 'no-cors',
    //     headers: { 'Content-Type': 'application/json' },
    //     body: JSON.stringify({
    //       email: emailInput.value,
    //       itinerary: itineraryInput.value,
    //       timestamp: new Date().toISOString(),
    //     }),
    //   });
    // } catch (err) {
    //   console.error('Submission error:', err);
    // }

    // 성공 화면 전환
    formSection.style.display = 'none';
    howSection.style.display = 'none';
    successMessage.classList.add('show');
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
</script>

</body>
</html>